# Galaxy-Web

Offline-first neon galaxy landing page prototype. Open `a-website/index.html` directly in a browser to explore. Keyboard shortcuts: `G` for hero, `S` for settings, `Esc` to close modals, `↑/↓` smooth scroll. Settings panel lets you tune the glow, blur, and star density live.
